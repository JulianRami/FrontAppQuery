/**
 * Represents a model for storing information about a saved query.
 */
export class QuerySavedModel {
  /**
   * The username associated with the saved query.
   */
  user: string = "";

  /**
   * The unique identifier assigned to the saved query.
   */
  idQuery: number = 0;
}
